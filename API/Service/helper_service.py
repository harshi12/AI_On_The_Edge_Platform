import argparse
import PlatformInputStream

class helperService:
    def __init__():

if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("--service_id", default = "-1")
    parser.add_argument("--run_on_gateway", default = "no")
    args = parser.parse_args()